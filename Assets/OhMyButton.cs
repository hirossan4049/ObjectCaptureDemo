using UnityEngine;

public class OhMyButton : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    public void OnClick()
    {
        Debug.Log("Oh my button was clicked!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
